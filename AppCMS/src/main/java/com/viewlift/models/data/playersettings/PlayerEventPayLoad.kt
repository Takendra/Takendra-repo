package com.viewlift.models.data.playersettings

/**
 * if we need more data in future we should create multiple constructor
 */
data class PlayerEventPayLoad(var playerEventType : Int, var  playerEventTime: Long, var  seekTime: Long)
